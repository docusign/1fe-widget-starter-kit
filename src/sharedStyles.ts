export const utilityCard = {
  width: "100%",
  maxWidth: "800px",
};

export const flexProps = {
  gap: 12,
  wrap: true,
};

export const colors = {
  primary: "#1890ff", // Used in 8+ components
  textSecondary: "#666", // Used in 5+ components
} as const;
